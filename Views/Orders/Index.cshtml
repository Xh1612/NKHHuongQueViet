@model List<Order>
<h1>Đơn hàng của tôi</h1>
@foreach (var o in Model)
{
    <div><a asp-action="Confirmation" asp-route-id="@o.Id">Đơn #@o.Id</a> — @o.Status — @o.TotalAmount.ToString("N0") đ</div>
}